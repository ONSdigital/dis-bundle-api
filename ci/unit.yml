---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-docker-go
    tag: latest

inputs:
  - name: dis-bundle-api

caches:
  - path: go/

run:
  path: bash
  args:
    - -exc
    - |
      /start_docker.sh
      dis-bundle-api/ci/scripts/unit.sh
